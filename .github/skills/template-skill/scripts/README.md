# Scripts

Use this folder for optional helper scripts that support the skill workflow.

Scripts placed here are ordinary files. They are not automatically registered as tools. If a workflow should use a script, explain when and how to use it from `SKILL.md`.

Good script candidates:

- Generate a report skeleton
- Transform an input file into a simpler format
- Run a repeatable local check
- Produce machine-readable output for the agent to inspect
